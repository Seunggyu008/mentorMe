import styled from 'styled-components';

const MentorListWrapper = styled.ul`
  margin: 10px;
  border: 1px solid black;
`;

const StyledList = styled.li`
  border: 1px solid black;
`;

export { MentorListWrapper, StyledList };
